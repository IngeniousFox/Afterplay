import { Library } from './screens/Library';

// Shell raíz de la app. De momento solo la biblioteca a pantalla completa —
// el rail de navegación lateral (SPEC 10.6) llega con el routing (Bloque 3A).
const Afterplay = (): React.JSX.Element => <Library />;

export default Afterplay;
